package codeEvalProblems;

public class PrimeNumber1000Sum {

	public static void main(String[] args){
		
		int number;
		int numberSum;
		int counter;

		while (counter>=1000; numberSum = 1){
		if((number.lastindexOf[0]=1) || (number.lastIndexOf(0) = 3) || (number.lastindexOf(0)=7) || (number.lastindexOf(0) =9))
		numberSum = numberSum + number;
		counter++;
		else 
			numberSum = numberSum + 0;
		}
	}
}
